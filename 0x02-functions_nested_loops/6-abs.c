#include "main.h"
/**
*_abs - competes the absoulute value of an integer
*@t: the integer to be competed
*Return: The absoulte value
*/

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
